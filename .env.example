# Create credentials in Aircall Dashboard → Integrations → API keys.
# Never commit real values.
AIRCALL_API_ID=
AIRCALL_API_TOKEN=

# Optional request timeout in milliseconds (default: 30000).
AIRCALL_TIMEOUT_MS=30000
